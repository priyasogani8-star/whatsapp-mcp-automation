# WhatsApp MCP Automation

This repository contains automation scripts for WhatsApp.

## Getting Started

To run the automation, execute `main.py` located in the `src` folder.

## Running Tests

Run the tests using:
```bash
python -m unittest discover -s tests
```
